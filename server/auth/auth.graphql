type Query {
  me: User!
  isLogin: Boolean!
}

type Mutation {
  login(username: String!, password: String!): User

  # logout: Boolean

  # register(
  #   username: String!,
  #   password: String!,
  # ): User
}
