type User {
  id: ID!
  session: String!
  username: String!
  profile: Profile!
  disabled: Boolean
  settings: Settings!
  createdAt: Date
  updatedAt: Date
}

type Settings {
  lng: String
  drawer: Boolean
}

type Mutation {

  synchronization: Boolean

}
