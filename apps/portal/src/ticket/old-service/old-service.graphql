type Category {
  code: String
  name: String
  description: String
  caterogyType: String
  avatar: String
}

type Service {
  code: String
  name: String
  group: String
  description: String
  avatar: String
  category: [Category!]!
}

type Query {
  GetService: [Service!]!
}

# type Mutation {

# }
