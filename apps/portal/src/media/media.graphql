type Media {
  id: ID
  createdAt: Date
  updatedAt: Date
  title: String
  file: String
  content: String
  user: User
}

type Query {
  mediaGet: [Media]
}

type Mutation {

  editMedia(title: String!, content: String, id: ID): Media

  deleteMedia(id: ID): Boolean

}
