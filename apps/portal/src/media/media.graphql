type Media {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
  title: String!
  file: String
  content: Upload
  user: User
}

type Query {
  mediaGet: [Media!]!
}

type Mutation {

  editMedia(title: String!, content: Upload, id: ID): Media

  deleteMedia(id: ID): Boolean

}
