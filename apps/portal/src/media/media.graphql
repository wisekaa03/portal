type File {
  id: ID!
  createdUser: User!
  updatedUser: User!
  createdAt: Date!
  updatedAt: Date!
  title: String!
  directory: String!
  filename: String!
  mimetype: String!
}

type Query {
  file(id: ID): [File!]!
}

type Mutation {
  editFile(attachment: Upload!, directory: String!, id: ID): File
  deleteFile(id: ID!): Boolean
}
