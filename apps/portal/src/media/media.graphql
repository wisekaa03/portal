type Media {
  id: ID!
  createdAt: Date!
  updatedAt: Date!
  title: String!
  file: String!
  content: Upload
  user: User!
}

type Query {
  media: [Media!]!
}

type Mutation {

  editMedia(content: Upload!, id: ID): Media

  deleteMedia(id: ID!): Boolean

}
