type Folder {
  id: ID!
  createdUser: User!
  updatedUser: User!
  createdAt: Date!
  updatedAt: Date!
  user: User
  pathname: String!
}

type Query {
  folder(id: ID): [Folder!]!
}

type Mutation {
  editFolder(pathname: String!, userId: ID, id: ID): Folder
  deleteFolder(id: ID!): Boolean
}
